import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'This App is Called AAAAAAAA!!!!!!!!',
  webDir: 'dist'
};

export default config;
